import { apelidoRand } from '../utils.js';

/**
 * Gera um apelido aleatório.
 * @returns {string} O apelido gerado.
 * @example
 * console.log(apelido()); // "Ace", "Alpha", etc.
 */
export function apelido() {
  return apelidoRand;
}
